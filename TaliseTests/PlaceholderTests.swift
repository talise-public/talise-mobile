// PlaceholderTests.swift
//
// The Sui gRPC client tests that used to live here were extracted into
// the SuiGrpcKit Swift package — see ios/SuiGrpcKit/Tests/SuiGrpcKitTests/.
// Run them with `swift test` from inside that package directory.
//
// This stub keeps the Talise app's test bundle target compilable so
// the Talise scheme still has a test action. New host-app-specific
// XCTests belong here.

import XCTest

final class PlaceholderTests: XCTestCase {
    func testPlaceholder() {
        XCTAssertTrue(true)
    }
}
